Cuando se envía un reporte mediante la ruta POST /reportes, la información se almacena temporalmente en el array reportes. Al consultar la ruta GET /reportes, el servidor devuelve todos los reportes registrados en formato JSON. Esto permite visualizar la lista de incidencias que han sido registradas mientras el servidor permanece en ejecución.
